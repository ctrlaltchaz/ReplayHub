#!/usr/bin/env node

/**
 * Passenger Entry Point for ReplayHub API (from startup folder)
 * Loads the built NestJS application
 */

// Go up one directory to project root, then into apps/api/dist/src
require('../apps/api/dist/src/main.js');
